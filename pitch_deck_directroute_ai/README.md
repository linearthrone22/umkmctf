# Pitch Deck — DirectRoute AI

Output deck:
- `output/DirectRouteAI_PitchDeck.pptx`

Source code (editable):
- `src/deck.mjs`

## Build ulang (opsional)

Jalankan dari folder ini:

```powershell
node .\src\deck.mjs
```

Catatan:
- Workspace ini butuh akses ke runtime `@oai/artifact-tool` yang dibundling oleh Codex.

